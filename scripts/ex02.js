function lancamento() {
    let numero = 10;
    let continuar = true;
    let numero = parseFloat(prompt("Digite um número (ou 'sair' para encerrar):"));
    if (numero < 0) {
       { else {
            aalert("Número negativo não é permitido para lançamento!");
       }}
        continuar = confirm("Deseja continuar lançando números?");
        }
    }
{
    alert("Lançamento realizado!");
}
   